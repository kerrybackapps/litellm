# Copy to .env for local testing (never commit .env)
OPENROUTER_API_KEY=sk-or-v1-...
LITELLM_MASTER_KEY=sk-litellm-mgmt675-2026   # shared with students
DATABASE_URL=postgresql://...                # optional: for persistent logs
